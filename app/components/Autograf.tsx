import styled from "styled-components";

const Autograf = styled.h1`
    position: absolute;
    margin: 0 auto;
    white-space: nowrap;
    font-family: Autograf; 
    font-size: 10vw;
    color: var(--secondary-color);
    z-index: 10;
    rotate: -20deg;
    opacity: 0.2;
`;

export default Autograf;